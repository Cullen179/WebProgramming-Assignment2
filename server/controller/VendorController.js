// TODO: import vendor model, handle route logic

class VendorController {
  // [GET] "/vendor/:id"
  getVendor(req, res, next) {
    // Handle logic get vendor by ID
  }

  // [POST] "/vendor"
  creatVendor(req, res, next) {
    // Handle logic create vendor
  }
}

module.exports = new VendorController();
