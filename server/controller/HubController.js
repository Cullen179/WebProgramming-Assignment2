// TODO: import hub model, handle route logic

class HubController {
  // [GET] "/hub/:id"
  getHub(req, res, next) {
    // Handle logic get hub by ID
  }

  // [POST] "/hub"
  creatHub(req, res, next) {
    // Handle logic create hub
  }
}

module.exports = new HubController();
